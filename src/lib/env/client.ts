// VidPortal v1 does not require browser-exposed environment variables.
// Add only explicitly public, non-secret NEXT_PUBLIC_* values here if that changes.
export const clientEnvironment = Object.freeze({});
